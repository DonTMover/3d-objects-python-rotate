[project]
name = "3d-objects-python-rotate"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiogram>=3.24.0",
    "fastapi>=0.128.0",
]

# Web app / server
dependencies_extra = [
    "fastapi>=0.98",
    "uvicorn[standard]>=0.22"
]

[tool.poetry.extras]
server = [
     "pyrender",
     "trimesh[speed]",
     "Pillow"
]

[tool.poetry.dependencies]
# Added for 3D rendering
numpy = "^1.28"
matplotlib = "^3.8"

:80 {
    handle_path /heartbeat* {
        reverse_proxy web:8000
    }
    handle_path /metrics* {
        reverse_proxy web:8000
    }
    handle_path /status* {
        reverse_proxy web:8000
    }
    handle_path /stats* {
        reverse_proxy web:8000
    }
    handle_path /api/* {
        reverse_proxy web:8000
    }

    # everything else served by frontend node
    reverse_proxy /* frontend:3000
}
